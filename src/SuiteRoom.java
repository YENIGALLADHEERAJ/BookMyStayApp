/**
 * CLASS - SuiteRoom
 * Represents a suite room in the hotel.
 */
public class SuiteRoom extends Room {

    public SuiteRoom() {
        // Calls the Room constructor with (beds, size, price)
        super(3, 750, 5000.0);
    }
}
